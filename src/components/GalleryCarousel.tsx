import { useState } from "preact/hooks";

const GalleryCarousel = () => {};

/* 
PSEUDO-CODE

What I need:
    * When click on image/thumbnail carousel opens in fullscreen
        * Image click connects to carousel (onClick?)
    * Image displays in full-size
    * Button (and maybe thumbnails at bottom) controls
        * Pressing btn/thumbnail fetches different image
*/
