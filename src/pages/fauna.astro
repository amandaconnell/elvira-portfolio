---
import Gallery from "../components/Gallery.astro"
import Layout from "../layouts/Layout.astro"

const pageTitle = "Fauna"
const category = "fauna"
---

<Layout pageTitle={pageTitle}>
    <Gallery pageTitle={pageTitle} category={category} />
</Layout>