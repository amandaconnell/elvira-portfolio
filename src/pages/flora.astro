---
import Gallery from "../components/Gallery.astro"
import Layout from "../layouts/Layout.astro"

const pageTitle = "Flora"
const category = "flora"
---

<Layout pageTitle={pageTitle}>
    <Gallery pageTitle={pageTitle} category={category} />
</Layout>