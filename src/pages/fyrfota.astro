---
import Gallery from "../components/Gallery.astro"
import Layout from "../layouts/Layout.astro"

const pageTitle = "Fyrfota"
const category = "fyrfota"
---

<Layout pageTitle={pageTitle}>
    <Gallery pageTitle={pageTitle} category={category} />
</Layout>